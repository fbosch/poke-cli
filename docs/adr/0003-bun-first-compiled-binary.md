# Use Bun-first development and compiled binary distribution

The Terminal Pokédex App will use Bun as the primary development runtime and target a compiled binary as the MVP distribution artifact. This fits the OpenTUI-oriented toolchain and gives users a simple runtime story, while accepting that npm package publication is not the primary delivery path for the first rebuild.
